# free-web-search

Performs a free web search using Google without requiring any API keys.

## Execute
Executes a web search and returns the top results.

Command:
node ./skills/free-web-search/search.js "{{query}}"

Arguments:
- query (string): The search query to look up on the web.
